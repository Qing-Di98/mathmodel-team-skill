# 更新日志

> 本文件只记录**本队（mathmodel-team-skill）自己的版本**。分叉前上游 math-modeling-skill 的完整历史见 `CHANGELOG_UPSTREAM.md`（仅作档案，不再更新）。
> 当前公开版本从 `1.0.0` 重新建立版本基线。根目录 `VERSION` 是当前版本的唯一准据。

## 1.1.0 - 2026-08-14

### 云端算力建议（AutoDL 提醒义务 + 公共镜像选择）

- 用户明确要求：代码阶段预估工作量太大时，skill 有义务提醒用户可在 AutoDL 上跑并给出公共镜像建议。新增 `references/云端算力建议.md`：**工作量评估标准**（预计 ≥30 分钟 / 需 GPU 大内存 / 本地环境不满足 / 数据量大 / 需并行，满足任一即提醒）；AutoDL **公共镜像选择四要素**（框架版本→CUDA 版本→Python 版本→预装环境，按任务类型推荐：深度学习选 PyTorch GPU 镜像、科学计算/仿真选 CPU 基础镜像、预处理选数据科学镜像）；上云前工程纪律（导出 requirements.txt、本地跑通最小样例、无路径硬编码、跑完释放实例、结果下载回本地写入 RESULTS_REPORT）。
- 挂载点：主 `SKILL.md` 强制协议第 10 条（提醒义务）；`stages/3coding-visual` 新增 **Step 1.5 工作量评估与云端算力提醒**；`references/README.md` 按任务加载表登记。

### 交互知情机制（建模与代码每关键 Step 用户参与知情，禁止黑箱推进）

- 用户明确要求：建模和代码的每一个关键 Step 都要让用户参与并知情、留思考空间。新增 `references/交互知情机制.md`：**两级交互**——知情层（每 Step 结束按【Step 结果】【含义】【下一步】三行式汇报 3-5 句，留思考空间）+ 决策层（模型选取/假设取舍/数据口径/参数确定/结果异常处理用 1.是/2.否/3.其他 确认）；**ARIMA/SARIMA 国赛七步逐步交互示范**（每步汇报什么、在哪步升级决策确认）；3coding-visual 每子问题完成即汇报、失败先报现象。
- 挂载点：主 `SKILL.md` 强制协议第 9 条；`stages/2analysis-modeling` 五个 Step 各加"用户参与"提示；`stages/3coding-visual` 四个 Step 各加"用户参与"提示；`references/README.md` 按任务加载表登记。

### 课件同步新分类 + 课件优先规则（第一优先学习资源）

- `references/课件/` 与源 `E:\数模\课件\` 分类对齐：新增 `数据处理与预测模型/`（`1数据预处理理论基础.docx`、`2ARIMA&SARIMA时间序列模型零基础全解.docx` 两个新增课件）与 `优化模型与评价模型/`（AHP/TOPSIS/bigM/PuLP代码/优化模型 + 评价模型 5 份 docx 移入子目录）。
- **全量 md 化**：新增 `extract_docx.py` 将全部 12 个课件 docx 提取为同名 `.md`（保留标题层级/正文/表格/代码），skill 可直接检索"学会"课件；新增 `课件/README.md`（课件索引 + 按任务加载表 + 课件内容速览：数据预处理/预测选型表、ARIMA 国赛 7 步流程、优化三要素、真题题型统计等）。
- **课件优先规则（用户明确定义，写进主 SKILL.md"课件优先"节与各阶段）**：模型使用及论文写作的学习资源**优先参考课件**；不同资料的模型术语、模型步骤等若有冲突，**一律以课件为准**（用户对课件有学习记忆和依赖）；课件未覆盖的内容才用其他资料补齐且沿用课件风格。挂载点：主 `SKILL.md`、`references/README.md` 全局规则、`stages/2analysis-modeling`（模型选取）、`stages/5writing`（写作标准）、`references/算法索引.md`（注记）。

### 新增模型假设与符号说明规范（写作教程 + 国一实例对照 + 校验工具）

- 新增 `references/模型假设和符号说明.md`：融合国赛论文写作教程《模型假设与变量说明》（知乎 p/717320007）与 2024/2025 四篇国一论文截图逐篇研读（24B 流水线、24C 乡村作物、25A 无人机烟幕、25B 碳化硅干涉）——假设内容要求与六种常见情况；假设格式 `**假设一**：假设……`（中文序号加粗直接陈述）；国一写法对照表（24B 附"说明："段 / 24C、25A 阿拉伯序号）；符号表三线表（符号/说明/单位，无单位 `-`，半页纸为宜，25B 式"如表 1 所示"标题在表格上方）；符号使用规范（科学下标、希腊字母数学模式、排序建议）。
- 新增 `stages/6verity/scripts/inline_math_check.py`：内联公式与科学下标校验工具（mathtext 渲染检查 + 转义星号/裸下标扫描 + 可选数值锚点完整性检查，`RESULT: FAIL` 即硬错误），在 2019 C 题简化版论文整改验证脚本基础上泛化；`stages/6verity` 新增 Step 1.5 挂载。
- 挂载点：`stages/5writing` 步骤 4（写模型假设与符号说明前必读）、`stages/6verity` Step 1.5（内联公式校验，终检必跑）。
- `references/README.md`"按任务加载"表登记新入口。

### Skill 结构审计升级（按"如何写好一个 Skill"指南逐项检查）

- 对照知乎指南《从"能用"到"会用"｜如何写好一个 Skill》（`E:\数模\Skill设计学习\如何写好一个Skill.md`）全面审计本仓库并补齐短板：
- **渐进式披露**：`tools/pdf-ocr/SKILL.md` 由 688 行拆分为 378 行中文主体 + `references/ENGLISH.md`（英文版按需加载），均加目录；全部 SKILL.md 降至 ≤500 行；14 个课件 md 全部加上"## 目录"（新增 `references/课件/add_toc.py` 幂等工具：标题语法/中文序号双模式提取，代码块与代码注释防护——课件 md 中 `#` 多为无围栏 Python 注释，`N.` 多为列表项，均不误判；PuLP代码.md 无可提取结构按警告跳过）。
- **评测回归纪律**：主 `SKILL.md` 强制协议新增第 11 条——修改本仓库（SKILL.md/脚本/模板/tests）后必须运行 `tests/` 回归，全部通过才算完成；新增行为先补评测用例（评测驱动、失败优先）。
- **回归修复**：`tests/` 全量 110 个测试首次跑通，修复融合遗留的测试断言过时（`test_optional_subagents_are_opt_in` 期望旧措辞"默认模式只派发"，融合版 SKILL.md 语义等价措辞为"不主动派发其他 Subagent"，已更新断言锁定当前语义）。

### 课件提取一步化（extract_docx.py 自动生成目录）

- `references/课件/extract_docx.py` 提取完 12 个 docx 后**自动调用 add_toc.py** 生成"## 目录"——课件更新流程由两步（extract_docx → add_toc）合并为一步；add_toc.py 保持独立可复用（幂等，也可单独重跑）。
- 验证：全量重跑后 12 个 md 与已提交版本逐字一致（提取→覆盖→重建 TOC 无漂移），`git diff` 仅剩脚本自身改动。
- 课件 README 更新流程同步简化。

## 1.0.4 - 2026-08-14

### 论文写作硬性要求再收紧（2026 年队内实践回填）

- `references/竞赛要求.md`"内容与结构"小节更新：假设条数以**五条为最佳、不超过六条**；假设模块**不涉及具体数值/参数类内容**、不得出现任何与后面代码/求解相关的实验过内容；假设格式为 `**假设一**：假设……`（中文序号加粗、逐条直接陈述，参照国一 24B 写法）；符号说明**汇总成一张三列表**（符号/说明/单位，每项居中，无单位用 `-`，以半页纸为宜、不超过一页，参照 24C/25B 三线表写法）；建模完**一定要做灵敏度分析**（参数扰动/场景变化对结论方向的影响）。
- "排版硬性细节"第 1 条补充：多个公式在一起时用大括号括起来，**大括号（公式组）不标号**；单行独立公式可标号。
- 触发源：2019 C 题简化版论文按上述要求整改（假设 5 条净化、符号表合并一张三列表、表格重编号、cases 公式组去编号、内联 LaTeX 科学下标全文替换）。

## 1.0.3 - 2026-08-14

### CHANGELOG 与上游仓库分离

- 新增 `CHANGELOG_UPSTREAM.md`：上游 math-modeling-skill 的全部历史（1.2.0/1.1.1/1.1.0/1.0.0 与版本号重置前 v2.4/v2.0/v1.0）移入该文件，仅作档案记录、不再更新；`CHANGELOG.md` 自此只保留本队条目。

### 补录当届官方论文格式规范（2026 年修订稿）

- 新增 `references/论文格式规范2026.md`：全国大学生数学建模竞赛组委会 2026 年修订稿全文（自发布之日起试行，与旧规范不相符处以新规范为准）。
- `references/竞赛要求.md` 硬性要求升级为纸质版/电子版/支撑材料三块完整提炼，重点突出**电子版硬性要求**：参赛论文单独一个文件（PDF 或 Word 之一，建议 PDF）、与纸质版完全一致、≤20MB、不压缩、**第一页必须为摘要专用页（不含承诺书和编号专用页）**；支撑材料 WinRAR 压缩（RAR/ZIP，≤20MB）、文件列表入附录、无支撑材料须注明。
- 挂载点：`stages/5writing`（写作前必读，电子版务必严格遵守）、`stages/6verity`（终检硬性检查项，不满足即硬错误）。

### 论文 PDF 仅在终稿确认后生成

- 原则：论文必然反复修改，写作与修改期间一律不生成最终 PDF，以 Word 草稿或 Typst/LaTeX 源码为工作形态；**仅在用户明确声明终稿后**才生成 PDF 并衔接当届电子版要求（单文件、与纸质版一致、≤20MB、不压缩、第一页为摘要专用页）。
- `stages/5writing/SKILL.md`：新增步骤 7"终稿确认与 PDF 生成"（终稿确认门禁 + 未确认时只报"草稿完成，待终稿确认"）；"LaTeX 写作要点→编译命令"加注记"编译仅限终稿确认后执行"。
- 主 SKILL.md 固定交付物：双解题手/单人模式均注明"PDF 仅在用户明确确认终稿后生成"。
- `stages/6verity/SKILL.md`：新增"终稿前提"——未确认终稿时只做源码/草稿级检查，终稿生成后才执行完整终检（含 PDF 逐页视觉检查与电子版要求核验）。

### 支撑材料打包先经用户过目清单

- 原则：支撑材料压缩前，主 Agent 先盘点候选文件（可运行源程序、自主查阅的数据资料、较大篇幅中间结果图表），**列文件清单给用户过目**（相对路径 + 用途，缺失项明确说明），用户确认或增删后才用 WinRAR 压缩为一个文件（RAR/ZIP，≤20MB）；未过目清单前不得直接压缩。
- `stages/5writing/SKILL.md`：新增步骤 8"支撑材料打包（先给用户过目清单，确认后再压缩）"（盘点 → 过目确认 → 压缩 → 校验，文件列表写入附录）。
- `stages/6verity/SKILL.md`：当届格式终检补充"支撑材料生成前先经用户确认清单"检查项。

### 新增摘要写作规范（国一实例逐条印证）

- 新增 `references/摘要写作规范.md`：整合国赛写作教程（标题与摘要篇）与 2024/2025 五篇国一摘要（24A 板凳龙、25A 无人机烟幕、25B 碳化硅干涉、25C NIPT 两篇）的研读成果——标题"基于XXX的XXX"一行内；摘要 3/4-1 页不加图表；开头段背景/做什么/意义三句（第二句最重要）；中间段每问"方法→关键处理→求解→精确数值"七条执行清单（数值必带单位、模型算法全名、交叉验证写相对误差、数据外引附录等）；总结段可选（30% 国一有，写则"创新性在于：一是…二是…"）；关键词 4-8 个核心模型词；写作时机与写后自检。
- 挂载点：`stages/5writing` 步骤 6"写摘要前必读并有意识地逐条执行"。
- `references/README.md` 按任务加载表登记。

### 新增流程图规范（模板图 + 反例对照）

- 新增 `references/流程图/`：流程图学习笔记（数模乐园 80 张流程图预览中 flow_2~6 的排版/配色/美观度分析——浅色分区块 + 一个主色相家族 + Problem/Task/Step 编号导航 + 短标签 + 三档字号层级）+ 五张模板图（flow_2~6.png）。
- 反例对照：量化分析 2019 简化版论文旧流程图（纯白底无分区色块、框内塞参数公式），明确**禁止**"草稿式"流程图。
- 挂载点：`stages/5writing` 步骤 3"构建图表规划"——流程图（研究思路/模型结构/求解流程）一律按本队模板标准绘制，绘图前必读笔记并对照模板图。
- `references/README.md` 按任务加载表登记。

### Requirements 新要点同步（排版硬性细节）

- `references/竞赛要求.md`：弹性需求补第 4 条——一旦出现数据处理，论文中**单开章节**呈现；硬性要求新增"排版硬性细节"小节（①多个公式用大括号括起不铺满一行；②符号下标必须科学排版，PDF 中禁止 `r_A` 这类带下划线写法；③表格标题在表格上方、流程图与数据可视化图标题在图下方，标题字号小于正文且一律黑色）。
- `stages/5writing/SKILL.md` 当届官方格式段补排版硬性细节摘要句；`stages/6verity` 终检按 `竞赛要求.md` 逐条检查自动生效。

## 1.0.2 - 2026-08-12

### 收录本队备赛课件（论文写作团队标准）

- 新增 `references/课件/`：收录 `E:\数模\课件\` 全部 11 份备赛资料（论文写作.md + 10 份 docx：备赛教案/新手指南/数模第一节课/建模思维与流程/CUMCM 国赛案例/优化+评价模型/AHP/TOPSIS/bigM/PuLP 代码）。
- 论文写作标准挂载：`stages/5writing/SKILL.md`"数学建模规范参考"小节新增本队写作标准——写作前必读 `references/课件/论文写作.md`，按其建议执行（摘要 300-400 字五要素、结构黄金模板与篇幅分配、符号先行/公式编号/三线表等）。
- `references/README.md`：新增"课件资料"导航小节 + "按任务加载"表登记论文写作团队标准入口。

### 融合 pdf-ocr-skill 与官方内置 skills（去芜存菁）

- 新增 `tools/pdf-ocr/`：收录 pdf-ocr-skill 完整源码（四引擎 OCR 脚本 + SKILL.md + requirements），用于影印版/图片题面与附件的中英文文字提取；挂载于 `stages/2analysis-modeling`（正常文本 PDF 仍用 `tools/pdf`）。
- `stages/5writing/SKILL.md` 新增"官方内置 Skills 调用登记"：英文论文（MCM/ICM）写作/润色/审稿/统计表述（nature-writing、nature-polishing、nature-reviewer、nature-statistics、nature-reader）、引用核验（nature-citation、nature-ref-verifier）、学术检索（nature-academic-search）、写作方法论（ml-paper-writing）、可视化规范（dataviz）、Word 生成（docx）、画布/协作排版（canvas-design、doc-coauthoring）——均为按需调用登记，**仓库内自足工具优先**（tools/paper_search、tools/figure、tools/docx、tools/pdf 等）。
- 主 SKILL.md 融合来源表登记两行（tools/pdf-ocr、官方内置 skills）；平台/开发工具（claude-api、update-config、keybindings 等）与数模领域无关，明确不纳入。
- `references/README.md` 工具表登记 `tools/pdf-ocr`。

### 解题手间互审省略，门禁统一由独立质检 Subagent 承担

- 背景：线下聚合比赛时解题手互不熟悉对方工作，互审质量无保障且占用对方求解时间，解题手间的交叉质检（M1/P1/P2 互审）取消。
- 主 SKILL.md：双解题手模式工作流中 M1、P1、P2 门禁执行者由"对方解题手担任独立质检官"改为"未参与编写的独立质检 Subagent 承担"（与单人模式一致），并注明互审省略的原因；并行边界"交叉质检只读"相应改为"独立质检只读"。
- `references/Subagent调度.md`：固定门禁表补充双解题手模式说明——M1/P1/P2 一律按单人模式派发未参与编写的质检 Subagent（只读 + 证据回执），不由对方解题手执行。
- 不变化的部分：阶段 2 结果锚点对账（数值一致性核对）仍由两人共同完成；协同问的双实现 + 交叉验证（结果数值一致性证据）保留，与互审无关。

## 1.0.1 - 2026-08-12

### 新增竞赛要求参考

- 新增 `references/竞赛要求.md`：本队整理的国赛通用要求——弹性需求（假设宁精勿多、前两问双模型交叉验证、评价模型选用）与硬性要求（论文格式规范：摘要 ≤1 页、无目录、正文 ≤30 页、参考文献规范；参赛作品构成：参赛论文 + 支撑材料及打包格式）。
- `references/README.md`"按任务加载"表登记新入口。

### 新增求解报告（每问求解完成后生成）

- 新增 `references/求解报告模板.md`：三部分结构——①求解中遇到的主要问题与改进方案；②出题人意图揣摩；③用通俗语言讲解建模方案与代码思路（面向非专业读者）。
- 定位为辅助文档：每子问题求解完成后生成 `求解报告_qN.md`，在求解全部完成之后给出，不构成门禁、不阻塞建模与求解流程。
- 挂载点：`stages/3coding-visual`（逐子问题清单第 7 项，单人模式）、主 SKILL.md 双解题手目录骨架（解题手工作区产出）。
- `references/README.md`"按任务加载"表登记新入口。

### 新增关键决策用户确认机制

- 新增 `references/决策确认机制.md`：AI 提出方案（附理由与备选）→ 用户选项 1.是 / 2.否 / 3.其他；必须等用户选择后才能进入下一步；确认结果记入契约。
- 重点覆盖初始建模：假设的提出与取舍、模型选取、数据口径；求解中的关键参数与方案取舍同样适用。
- 挂载点：`stages/2analysis-modeling`（工作流开头说明，单人模式）、主 SKILL.md 阶段 0 工作流与并行边界（契约冻结以用户确认为前提）。
- `references/README.md`"按任务加载"表登记新入口。

### 新增解题手分工建议（协同/独立灵活模式）

- 新增 `references/分工建议.md`：阶段 0 完整读题后按依赖结构自适应选择模式——**完全独立**（0 基础问，按题号各自独立并行）/ **协同 + 独立混合**（1 基础问，基础问双实现交叉验证、其余独立）/ **协同为主**（链式/多基础问，承接链分段协同、末端独立）。
- 共同底料：轻度协同（共享契约 + 锚点对账）在所有模式下保留；协同问双实现、独立问工作区隔离；结果一致作为交叉验证证据。
- 实力分配（一强一弱）：强者主导协同问并承担较难独立问，弱者承担较简单独立问并参与协同问交叉验证；分工建议经用户确认后冻结。
- 思路与代码开放原则：两解题手的思路文档、代码、结果与求解报告对论文手（合稿方）全程开放。
- 挂载点：主 SKILL.md 阶段 0 工作流（分工建议步骤）与并行边界（开放原则）。
- `references/README.md`"按任务加载"表登记新入口。

## 1.0.0 - 2026-08-12

### 团队融合版（本仓库首个版本）

- 融合 math-modeling-skill v1.2.0 与 MathModelAgent skills 两套体系，同质内容择优（科学准确、全面及时）。
- 新增融合入口 `SKILL.md`（name: mathmodel-team）：双解题手并行工作流（共享契约冻结 → 并行求解 + 交叉质检 → 结果锚点对账 → 集中合稿 → 终检）与单人模式双轨。
- 新增 `references/共享契约/` 六件模板：题目分析报告、术语表格、假设清单、符号表、数据口径、结果锚点表。
- 原两套目录原样搬入 `stages/`、`tools/`、`references/roles/`，内部相对引用不变；仅 3 处同质文档头部加"融合注记"（常见模式/MATLAB规范/3coding-visual）。
- 版本基线：本仓库独立编号，与上游 math-modeling-skill v1.2.0 的变更历史不混排。

## 版本规则

采用语义化版本 `MAJOR.MINOR.PATCH`：

- `MAJOR`：不兼容变更，例如修改固定交付物、目录契约、命令参数或复现清单结构。
- `MINOR`：向后兼容的新能力，例如新增算法类别、工具功能或竞赛配置。
- `PATCH`：向后兼容的修复，例如错误修正、文档校正和测试补充。

预发布版本使用 `1.2.0-beta.1`；Git 标签使用 `v1.1.1`。
